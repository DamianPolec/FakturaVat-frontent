import { Routes } from '@angular/router';
import { InvoiceComponent } from './feature-modules/invoice/components/invoice/invoice.component';

export const routes: Routes = [
    {
        path: '',
        component: InvoiceComponent
    }
];
